<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

        <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="DefaultIncreaseRepeatButtonStyle.xaml"></ResourceDictionary>
            <ResourceDictionary Source="DefaultDecreaseRepeatButtonStyle.xaml"></ResourceDictionary>
            <ResourceDictionary Source="DefaultSliderThumbStyle.xaml"></ResourceDictionary>
        </ResourceDictionary.MergedDictionaries>
    
    <ControlTemplate x:Key="DefaultHorizontalSlider"
                     TargetType="{x:Type Slider}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TickBar Grid.Row="2" x:Name="BottomTickBar"
                     SnapsToDevicePixels="True"
                     Placement="Bottom"
                     Margin="0, -5, 0, 0"
                     Height="10"
                     Visibility="Visible">
                <TickBar.Fill>
                    <SolidColorBrush Color="#6789A5" />
                </TickBar.Fill>
            </TickBar>
            <Track Grid.Row="1"
                   x:Name="PART_Track">
                <Track.DecreaseRepeatButton>
                    <RepeatButton Style="{StaticResource DefaultDecreaseRepeatButtonStyle}"
                                  Command="Slider.DecreaseLarge" />
                </Track.DecreaseRepeatButton>
                <Track.Thumb>
                    <Thumb Style="{StaticResource DefaultSliderThumbStyle}" />
                </Track.Thumb>
                <Track.IncreaseRepeatButton>
                    <RepeatButton Style="{StaticResource DefaultIncreaseRepeatButtonStyle}"
                                  Command="Slider.IncreaseLarge" />
                </Track.IncreaseRepeatButton>
            </Track>
        </Grid>
    </ControlTemplate>

</ResourceDictionary>