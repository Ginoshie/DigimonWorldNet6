<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="DefaultSliderThumbStyle" TargetType="{x:Type Thumb}">
        <Setter Property="SnapsToDevicePixels"
                Value="true" />
        <Setter Property="OverridesDefaultStyle"
                Value="true" />
        <Setter Property="Height"
                Value="18" />
        <Setter Property="Width"
                Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Thumb}">
                    <Grid>
                        <Border x:Name="OuterBorder" BorderBrush="#B0B0B0" BorderThickness="1,1,0,0" Background="#575C60">
                            <Border x:Name="InnerBorder" BorderBrush="#060100" BorderThickness="0,0,2,2" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseCaptured" Value="True">
                            <Setter TargetName="OuterBorder" Property="BorderBrush" Value="#060100" />
                            <Setter TargetName="OuterBorder" Property="BorderThickness" Value="2,2,0,0" />
                            <Setter TargetName="InnerBorder" Property="BorderBrush" Value="#6789A5" />
                            <Setter TargetName="InnerBorder" Property="BorderThickness" Value="0,0,1,1" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>