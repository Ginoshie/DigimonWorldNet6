<UserControl x:Class="DigimonWorld.Frontend.WPF.Windows.Main.UserControls.Panes.EmulatorLinkRightPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:main="clr-namespace:DigimonWorld.Frontend.WPF.Windows.Main"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:emulatorLink="clr-namespace:DigimonWorld.Frontend.WPF.Windows.Main.UserControls.EmulatorLink"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=main:MainWindowViewModel, IsDesignTimeCreatable=True}">

    <UserControl.Resources>
        <ResourceDictionary>

            <GridLength x:Key="PanelLabelThickness">45</GridLength>
            <GridLength x:Key="RightPanelWidth">420</GridLength>

            <system:Double x:Key="RightPanelYOffset">-420</system:Double>

            <BitmapImage x:Key="ClosedRightPaneImage" UriSource="pack://application:,,,/Frontend.WPF;component/Images/ComponentLabels/label-right-no-focus.png" />
            <BitmapImage x:Key="OpenRightPaneImage" UriSource="pack://application:,,,/Frontend.WPF;component/Images/ComponentLabels/label-right-focus.png" />

            <Style x:Key="RightPaneLabelStyle" TargetType="Image">
                <Setter Property="Source" Value="{StaticResource ClosedRightPaneImage}" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RightPaneViewModelComponent.RightPaneIsOpen}" Value="True">
                        <Setter Property="Source" Value="{StaticResource OpenRightPaneImage}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

            <Storyboard x:Key="OpenRightPane">
                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                 To="-12" Duration="{StaticResource PanelAnimationDuration}" />
            </Storyboard>
            <Storyboard x:Key="CloseRightPane">
                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                 To="{StaticResource RightPanelYOffset}" Duration="{StaticResource PanelAnimationDuration}" />
            </Storyboard>

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="Transparent" VerticalAlignment="Center" SnapsToDevicePixels="True" UseLayoutRounding="True" Panel.ZIndex="0">
        <Grid.RenderTransform>
            <TranslateTransform X="{StaticResource RightPanelYOffset}" />
        </Grid.RenderTransform>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{StaticResource RightPanelWidth}" />
            <ColumnDefinition Width="{StaticResource PanelLabelThickness}" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Background="Transparent" Panel.ZIndex="1">
            <Border CornerRadius="8" BorderBrush="Black" BorderThickness="3" Padding="0">
                <Border CornerRadius="8" BorderBrush="#4A9FC5" BorderThickness="3" Margin="0" Padding="0">
                    <Border CornerRadius="5" BorderBrush="#90D9FA" BorderThickness="3" Padding="0">
                        <Border CornerRadius="5" BorderBrush="Black" BorderThickness="3" Padding="0" Background="#2E3841">
                            <emulatorLink:EmulatorLinkUserControl HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0 10, 10, 10" />
                        </Border>
                    </Border>
                </Border>
            </Border>
        </Border>

        <Grid Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Background="Transparent">
            <Image Style="{StaticResource RightPaneLabelStyle}" Margin="-2,0, 0, 0">
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="MouseLeftButtonDown">
                        <b:InvokeCommandAction Command="{Binding RightPaneViewModelComponent.ToggleRightPaneCommand}" />
                    </b:EventTrigger>
                </b:Interaction.Triggers>
            </Image>

            <TextBlock Text="E&#x0a;m&#x0a;u&#x0a;l&#x0a;a&#x0a;t&#x0a;o&#x0a;r&#x0a; &#x0a;L&#x0a;i&#x0a;n&#x0a;k"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontWeight="Bold"
                       TextAlignment="Center">

                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="FontFamily" Value="{StaticResource Dw1Font}" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Setter Property="Foreground" Value="#D6D6D8" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding RightPaneViewModelComponent.RightPaneIsOpen}" Value="True">
                                <Setter Property="Foreground" Value="Black" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
                <b:Interaction.Triggers>
                    <b:EventTrigger EventName="MouseLeftButtonDown">
                        <b:InvokeCommandAction Command="{Binding RightPaneViewModelComponent.ToggleRightPaneCommand}" />
                    </b:EventTrigger>
                </b:Interaction.Triggers>
            </TextBlock>
        </Grid>

        <Grid.Style>
            <Style TargetType="Grid">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding RightPaneViewModelComponent.RightPaneIsOpen}" Value="True">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard Storyboard="{StaticResource OpenRightPane}" />
                        </DataTrigger.EnterActions>
                        <DataTrigger.ExitActions>
                            <BeginStoryboard Storyboard="{StaticResource CloseRightPane}" />
                        </DataTrigger.ExitActions>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
    </Grid>

</UserControl>