<UserControl x:Class="DigimonWorld.Frontend.WPF.Windows.Main.UserControls.DigiWiki.UserControls.FoodTopic.FoodTopicUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:foodTopic="clr-namespace:DigimonWorld.Frontend.WPF.Windows.Main.UserControls.DigiWiki.UserControls.FoodTopic"
             Width="1011"
             Height="554"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=foodTopic:FoodTopicViewModel, IsDesignTimeCreatable=True}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="230"></ColumnDefinition>
            <ColumnDefinition Width="650"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="300"></RowDefinition>
        </Grid.RowDefinitions>
        <Image Grid.Row="0" Grid.Column="0" Height="175" Source="/Images/Digimon/Shellmon-right.png" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,15,10,15"/>
            
        <Border Grid.Row="0" Grid.Column="1" CornerRadius="8" BorderBrush="Black" BorderThickness="3" Margin="0, 15,15, 10" Padding="0" Background="Black">
            <b:Interaction.Triggers>
                <b:EventTrigger EventName="MouseLeftButtonDown">
                    <b:InvokeCommandAction Command="{Binding InstantDisplayCommand}" />
                </b:EventTrigger>
            </b:Interaction.Triggers>
            <Border CornerRadius="8" BorderBrush="#4A9FC5" BorderThickness="3" Padding="0" Background="#4A9FC5">
                <Border CornerRadius="5" BorderBrush="#90D9FA" BorderThickness="3" Padding="0" Background="#2E3841">
                    <Border CornerRadius="5" BorderBrush="Transparent" BorderThickness="0" Padding="0" Background="#77000000">
                        <StackPanel Margin="5">
                            <TextBlock Text="Shellmon"
                                       Foreground="#D4D604"
                                       FontSize="15"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Margin="0,5, 0, 5"
                                       Width="545"
                                       TextWrapping="Wrap" />
                            <TextBlock Text="{Binding ShellmonText}"
                                       Foreground="#B3B5B1"
                                       FontSize="14"
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Margin="0, 5"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </Border>
            </Border>
        </Border>
        
        <Border Grid.Row="1" Grid.Column="1" BorderBrush="#060100" BorderThickness="3,3,0,0" Margin="15" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Border BorderBrush="#6789A5" BorderThickness="0,0,3,3" Background="#354A5B">
                <Image Width="300" Height="300" Source="{Binding DigimonType, Converter={StaticResource EnumToImageConverter}}" Stretch="Uniform" />
            </Border>
        </Border>
    </Grid>
</UserControl>
