# Copilot Instructions

## Formatting

- New files should not end with blank lines.
- `else`, `else if`, and `catch` should be placed directly after the closing `}` of the preceding block, with no blank line in between.
- if and else blocks always have braces, even if they contain only a single statement.
